<Window x:Class="Quoc_MEP.AlignBranchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revit MEP Tool - Căn chỉnh nhánh | Align Branch" 
        Width="340" Height="240"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Chọn trục căn chỉnh | Select alignment axes:" 
                   FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <!-- Checkbox X -->
        <CheckBox Grid.Row="1" x:Name="cbAlignX" Content="  X  —  Căn trái/phải (Left/Right)" 
                  FontSize="12" Margin="8,4" IsChecked="False"/>

        <!-- Checkbox Y -->
        <CheckBox Grid.Row="2" x:Name="cbAlignY" Content="  Y  —  Căn trên/dưới (Up/Down)" 
                  FontSize="12" Margin="8,4" IsChecked="False"/>

        <!-- Checkbox Z -->
        <CheckBox Grid.Row="3" x:Name="cbAlignZ" Content="  Z  —  Căn cao độ (Elevation)" 
                  FontSize="12" Margin="8,4" IsChecked="False"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="80" Height="28" Margin="0,0,8,0" 
                    IsDefault="True" Click="BtnOK_Click"/>
            <Button Content="Cancel" Width="80" Height="28" 
                    IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
